export const UserLocation = (props) => {

    return (
        <>
            <p>Country:{props.userlocation.country} State:{props.userlocation.state} City:{props.userlocation.city}</p>
        </>
    )
}